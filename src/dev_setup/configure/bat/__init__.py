"""The `devstuff configure bat` wizard — bat's config file and shell integration.

bat's config file is a list of the same long options the command line takes, one per
line, so this configurator is the closest of the four to starship's: the real binary
renders the candidate exactly, and the preview is the deliverable.
"""
