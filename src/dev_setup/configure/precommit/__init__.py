"""The `devstuff configure pre-commit` wizard.

The catalog key is `pre-commit`, which is not a legal Python identifier — hence the
package name `precommit` and the hyphenated key in `configure.CONFIGURATORS`.
"""
