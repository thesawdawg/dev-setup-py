services:
  sandbox:
    build:
      context: ..
      dockerfile: dev/Dockerfile
    image: devstuff-sandbox
    stdin_open: true
    tty: true
    volumes:
      # Persist custom packages added via 'devstuff add' across container runs
      - devstuff-config:/home/developer/.config/devstuff

volumes:
  devstuff-config:
